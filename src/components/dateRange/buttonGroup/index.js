import React from 'react'

import { AppBar, Button, Box, Typography, IconButton } from '@material-ui/core';

function ButtonGroup() {
  return (
    <Box display="flex" flexDirection="column">
      <Button>
        Save
      </Button>
    </Box>
  )
}

export default ButtonGroup
